conky.config = {
	alignment = 'middle_middle',
	background = true,
	border_width = 0,
	color2 = 'gray50',
	cpu_avg_samples = 1,
	default_color = 'white',
	default_outline_color = 'white',
	default_shade_color = 'white',
	double_buffer = true,
	draw_borders = false,
	draw_graph_borders = false,
	draw_outline = false,
	draw_shades = false,
	font = 'Source Code Pro:size=30',
	gap_x = 0,
	gap_y = 0,
	lua_draw_hook_pre = 'draw_bg',
	lua_load = '~/.conky/draw_bg.lua',
	net_avg_samples = 1,
	no_buffers = false,
	own_window = true,
	own_window_argb_visual = true,
	own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
	own_window_transparent = true,
	own_window_type = 'desktop',
	total_run_times = 0,
	update_interval = 1,
	use_spacer = 'left',
	use_xft = true,
}

conky.text = [[
	${font Source Code Pro:size=120}${time %H:%M}${font}
	${font Source Code Pro:size=60}${time %A}${font}
	${font Source Code Pro:size=60}${time %B%e}${font}

	${color2}Los Angeles${color} ${tztime America/Los_Angeles %H:%M}
	${color2}Chicago${color}     ${tztime America/Chicago %H:%M}
	${color2}Raleigh${color}     ${tztime America/New_York %H:%M}
	${color2}Recife${color}      ${tztime America/Recife %H:%M}
	${color2}Dublin${color}      ${tztime Europe/Dublin %H:%M}
	${color2}Madrid${color}      ${tztime Europe/Madrid %H:%M}
	${color2}Dubai${color}       ${tztime Asia/Dubai %H:%M}
	${color2}Bangalore${color}   ${tztime Asia/Kolkata %H:%M}
	${color2}Dalian${color}      ${tztime Asia/Shanghai %H:%M}
	${color2}Tokyo${color}       ${tztime Asia/Tokyo %H:%M}
	${color2}Sydney${color}      ${tztime Australia/Sydney %H:%M}

	${color2}Battery${color}     ${battery_percent}%
]]